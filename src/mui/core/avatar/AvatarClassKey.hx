package mui.core.avatar;

@:enum abstract AvatarClassKey(String) to String {
	var Root = "root";
	var ColorDefault = "colorDefault";
	var Fallback = "fallback";
	var Circle = "circle";
	var Rounded = "rounded";
	var Square = "square";
	var Img = "img";
}
