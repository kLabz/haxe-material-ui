package mui.core.form;

@:enum abstract FormControlClassKey(String) to String {
	var Root = "root";

	var MarginNormal = "marginNormal";
	var MarginDense = "marginDense";
	var FullWidth = "fullWidth";
}
