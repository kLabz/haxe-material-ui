package mui.core.list;

@:enum abstract ListSubheaderClassKey(String) to String {
	var Root = "root";
	var ColorPrimary = "colorPrimary";
	var ColorInherit = "colorInherit";
	var Gutters = "gutters";
	var Inset = "inset";
	var Sticky = "sticky";
}
